name: deploy-to-staging

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    name: Run a multi-line script
    runs-on: ubuntu-latest
    steps:
        env:
          SUPER_SECRET: ${{ secrets.SuperSecret }}
        run: |
          docker build ${{ secrets.SuperSecret }} .
